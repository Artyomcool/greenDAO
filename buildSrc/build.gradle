apply plugin: 'groovy'

dependencies {
  compile gradleApi()
  compile localGroovy()
}

allprojects {
  apply plugin: 'idea'
  apply plugin: 'maven'
  
  group = 'com.lemoulinstudio.greendao'
  version = '1.3.1'
  
  repositories {
    mavenCentral()
    mavenLocal()
  }
}
